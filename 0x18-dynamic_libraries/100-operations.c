#include "100-operations.h"

/**
 * add - operation fxn
 * @a: first number
 * @b: second number
 * Return: results
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - operation fxn
 * @a: first number
 * @b: second number
 * Return: results
 */
int sub(int a, int b)
{
	return (a - b);
}

/**
 * mul - operation fxn
 * @a: first number
 * @b: second number
 * Return: results
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 * div - operation fxn
 * @a: first number
 * @b: second number
 * Return: results
 */
int div(int a, int b)
{
	return (a / b);
}

/**
 * mod - operation fxn
 * @a: first number
 * @b: second number
 * Return: results
 */
int mod(int a, int b)
{
	return (a % b);
}
