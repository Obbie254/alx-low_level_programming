#include <stdio.h>

int main()
{
  /* initializing string */
  char str[] = "script that compiles a C file and creates an executable named cisfun.";

  /* writing to stdout */
  puts(str);

  return(0);
}
