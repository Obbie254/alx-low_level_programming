#include <stdio.h>

/* Fxn that uses puts() to print.
 * Fxn return zero.
 */
int main()
{
  /* initializing string */
  char str[] = "\"Programming is like building a multilingual puzzle";

  /* writing to stdout */
  puts(str);

  return(0);
}
